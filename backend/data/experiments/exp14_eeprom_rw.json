{
  "id": "exp14_eeprom_rw",
  "title": "Level 14: Non-Volatile Memory (EEPROM)",
  "difficulty": "Advanced",
  "aim": "To durably store and retrieve configuration variables on the ATmega328P through power cycles using internal EEPROM.",
  "objective": "Understand the EEPROM Address (EEAR), Data (EEDR), and Control (EECR) registers, and safely observe hardware wait-states for erase/write cycles.",
  "theory": "<h3>What is EEPROM?</h3><p>Most memory (SRAM) is erased instantly when the power turns off. The ATmega328P contains 1024 bytes (1 KB) of Electrically Erasable Programmable Read-Only Memory (EEPROM), which retains data completely without power.</p><h3>Writing to EEPROM</h3><p>Writing to EEPROM is slow (3.3ms) because it physically alters trapped electrons. When the power is lost during a write, EEPROM corrupts. To write a byte:<br>1. Wait until any previous write is finished (poll <b>EEPE</b> inside <b>EECR</b>).<br>2. Load the physical memory address (0-1023) into <b>EEAR</b>.<br>3. Load the 8-bit data into <b>EEDR</b>.<br>4. Temporarily disable interrupts using `cli()`.<br>5. Set the Master Write Enable bit (<b>EEMPE</b>) in EECR.<br>6. Within exactly 4 clock cycles, set the Write Enable bit (<b>EEPE</b>).</p><h3>Reading from EEPROM</h3><p>Reading is extremely fast compared to writing. You wait until write operations are complete, supply the address into EEAR, set the Read Enable bit (EERE), and then copy the resulting byte pulled into EEDR.",
  "pretest": [
    {
      "question": "Why is it mandatory to run 'cli()' (Clear Interrupts) before triggering the Master Write Enable (EEMPE) bit?",
      "options": ["To avoid corrupting the UART hardware", "Because the final EEPE write must occur within exactly 4 clock cycles, and an ISR triggering in between would cause the write to be aborted", "To speed up the EEPROM oscillator", "To clear the previous data from the registers"],
      "correct_answer_index": 1,
      "explanation": "The 4-cycle timeout is a strict hardware security measure against accidental erratic writes when voltage drops."
    }
  ],
  "procedure": [
    "Connect a Push Button to Pin 2 and an LED to Pin 13.",
    "Write an EEPROM_Write(uint16_t uiAddress, uint8_t ucData) function according to the theory rules, including the cli() macro.",
    "Write an EEPROM_Read(uint16_t uiAddress) function.",
    "In your setup() function, read the EEPROM address '0'. If it is a '1', turn the LED ON. If '0', keep it OFF.",
    "In the main loop, if the button is pressed, toggle the LED state (ON to OFF, or OFF to ON).",
    "Immediately save this new state ('0' or '1') to EEPROM address '0' using EEPROM_Write().",
    "Reset the microcontroller to verify that it remembers the state of the LED before the reset occurred."
  ],
  "posttest": [
    {
      "question": "What is the physical maximum amount of write/erase cycles guaranteed for an EEPROM cell in an ATmega328P before it potentially fails?",
      "options": ["1,000", "10,000", "100,000", "Infinite"],
      "correct_answer_index": 2,
      "explanation": "Atmel rates the EEPROM for 100,000 erase/write cycles. Writing to it continuously inside an infinite loop will destroy it in just minutes."
    }
  ],
  "feedback": "Memory is persistent! You now know how to save user settings, high scores, or calibration sensor constants directly on the chip."
}
